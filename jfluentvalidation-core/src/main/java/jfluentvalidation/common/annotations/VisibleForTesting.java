package jfluentvalidation.common.annotations;

/**
 * Indicates a member is more widely visible than otherwise necessary, only for use in test code.
 * From Google's Guava library
 */
public @interface VisibleForTesting {}
